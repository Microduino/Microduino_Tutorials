一个CC3000、一个OLED、库用的是Adafruit_CC3000，程序写的有点傻，内存占用很大，我只用了1284调试，不保证644能用（不优化的前提下328更不可能）。

v1.3优化了内存，644可以很安稳的跑了，但328依旧不行~（请下我改过的库）

v1.4加入了“cc3000.stop()”语句，以实现休眠功能，mega1284实测，整个系统运行150ma，休眠后小于40ma。

v2.1加入了Smartconfig功能，若换了一个新环境，会自动进入该模式